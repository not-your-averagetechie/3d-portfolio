import { useState, useCallback } from "react";
import "./styles/Work.css";
import WorkImage from "./WorkImage";
import { MdArrowBack, MdArrowForward } from "react-icons/md";

const projects = [
  {
    title: "Accounts Executive Internship",
    category: "Financial Operations & Compliance",
    tools: "Tally ERP, GST Filing, Bank Reconciliation, Ledger Management",
    image: "/images/accounting.png",
    link: "#",
  },
  {
    title: "NISM Mutual Fund Distributor",
    category: "Professional Certification",
    tools: "Asset Allocation, Risk Evaluation, Mutual Fund Structures",
    image: "/images/nism_cert.png",
    link: "#",
  },
  {
    title: "Excel for Business Analytics",
    category: "Data Analysis Certification",
    tools: "Advanced Excel, Pivot Tables, Data Modeling, Financial Analysis",
    image: "/images/excel_analytics.png",
    link: "#",
  },
  {
    title: "Power BI for Data Analysis",
    category: "Visualization Certification",
    tools: "Data Visualization, DAX, Interactive Dashboards, SQL Integration",
    image: "/images/powerbi_cert.png",
    link: "#",
  },
  {
    title: "AI Tool Workshop",
    category: "Modern Technology Certification",
    tools: "AI Implementation, Productivity Tools, Automation Workflows",
    image: "/images/ai_workshop.png",
    link: "#",
  },
  {
    title: "NISM SEBI Awareness",
    category: "Market Regulatory Certification",
    tools: "Market Regulations, Investor Awareness, Financial Compliance",
    image: "/images/sebi_awareness.png",
    link: "#",
  }
];

const Work = () => {
  const [currentIndex, setCurrentIndex] = useState(0);
  const [isAnimating, setIsAnimating] = useState(false);

  const goToSlide = useCallback(
    (index: number) => {
      if (isAnimating) return;
      setIsAnimating(true);
      setCurrentIndex(index);
      setTimeout(() => setIsAnimating(false), 500);
    },
    [isAnimating]
  );

  const goToPrev = useCallback(() => {
    const newIndex =
      currentIndex === 0 ? projects.length - 1 : currentIndex - 1;
    goToSlide(newIndex);
  }, [currentIndex, goToSlide]);

  const goToNext = useCallback(() => {
    const newIndex =
      currentIndex === projects.length - 1 ? 0 : currentIndex + 1;
    goToSlide(newIndex);
  }, [currentIndex, goToSlide]);

  return (
    <div className="work-section" id="work">
      <div className="work-container section-container">
        <h2>
          Featured <span>Work & Certs</span>
        </h2>

        <div className="carousel-wrapper">
          {/* Navigation Arrows */}
          <button
            className="carousel-arrow carousel-arrow-left"
            onClick={goToPrev}
            aria-label="Previous project"
            data-cursor="disable"
          >
            <MdArrowBack />
          </button>
          <button
            className="carousel-arrow carousel-arrow-right"
            onClick={goToNext}
            aria-label="Next project"
            data-cursor="disable"
          >
            <MdArrowForward />
          </button>

          {/* Slides */}
          <div className="carousel-track-container">
            <div
              className="carousel-track"
              style={{
                transform: `translateX(-${currentIndex * 100}%)`,
              }}
            >
              {projects.map((project, index) => (
                <div className="carousel-slide" key={index}>
                  <div className="carousel-content">
                    <div className="carousel-info">
                      <div className="carousel-number">
                        <h3>0{index + 1}</h3>
                      </div>
                      <div className="carousel-details">
                        <h4>{project.title}</h4>
                        <p className="carousel-category">
                          {project.category}
                        </p>
                        <div className="carousel-tools">
                          <span className="tools-label">Key Expertise</span>
                          <p>{project.tools}</p>
                        </div>
                      </div>
                    </div>
                    <div className="carousel-image-wrapper">
                      <WorkImage
                        image={project.image}
                        alt={project.title}
                        link={project.link}
                      />
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Dot Indicators */}
          <div className="carousel-dots">
            {projects.map((_, index) => (
              <button
                key={index}
                className={`carousel-dot ${index === currentIndex ? "carousel-dot-active" : ""
                  }`}
                onClick={() => goToSlide(index)}
                aria-label={`Go to project ${index + 1}`}
                data-cursor="disable"
              />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default Work;